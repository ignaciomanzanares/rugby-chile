DATABASE_URL="postgresql://rugby_user:rugby_pass@localhost:5432/rugby_chile"
PORT=4000
WEB_URL="http://localhost:3000"

# OBLIGATORIO — secreto para firmar los cookies de sesión (JWT). La API NO
# arranca sin esto (fail-closed, sin default). Generá uno fuerte con:
#   openssl rand -hex 32
JWT_SECRET=""

# Instagram lineup scraper — self-healing login (use a DEDICATED throwaway
# account, never a personal one). The app logs in, persists the session to
# .ig-session.json and re-logs-in automatically when it expires.
IG_USERNAME=""
IG_PASSWORD=""
